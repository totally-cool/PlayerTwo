# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Tauri updater signing keys — NEVER commit the private key
/.tauri/
*.key

# Private account / login data — NEVER commit migrated or saved profiles.
# (The real store lives in %APPDATA%\PlayerTwo; these patterns guard
#  against a data dir ever being pointed inside the repo, plus test imports.)
/data/
**/accounts/
**/LoginCache/
*.profile
GameUserSettings.ini
reg.json
registry.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
